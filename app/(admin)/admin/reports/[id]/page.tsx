import { notFound } from "next/navigation";import { Card, CardContent } from "@/components/ui/card";import { StatusBadge } from "@/components/admin/shared/StatusBadge";import { ReplyForm } from "@/components/admin/reports/ReplyForm";import { markReportRead } from "@/lib/actions/admin";import { prisma } from "@/lib/prisma";
export const dynamic = 'force-dynamic'

export default async function ReportDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const report=await prisma.report.findUnique({where:{id},include:{sender:true,receiver:true}});if(!report) notFound();if(report.status==='UNREAD') await markReportRead(report.id);const student=report.studentId?await prisma.studentProfile.findUnique({where:{id:report.studentId},include:{user:true}}):null;return <div className='space-y-4'><Card><CardContent className='p-6'><h1>{report.title}</h1><p>From: {report.sender.firstName} {report.sender.lastName}</p><p>{report.createdAt.toLocaleDateString()}</p><StatusBadge status={report.status}/>{student&&<p>Student: {student.user.firstName} {student.user.lastName}</p>}</CardContent></Card><Card><CardContent className='p-6 whitespace-pre-wrap'>{report.content}</CardContent></Card><ReplyForm report={report}/></div>}
