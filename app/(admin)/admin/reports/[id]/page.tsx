import { prisma } from "@/lib/prisma";import { replyToReport } from "@/lib/actions/admin";import { redirect } from "next/navigation";
export default async function ReportDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const r=await prisma.report.findUnique({where:{id},include:{sender:true}});if(!r) return null; if(r.status==='UNREAD') await prisma.report.update({where:{id},data:{status:'READ'}}); async function action(fd:FormData){"use server";await replyToReport(id,String(fd.get('content')??''));redirect('/admin/reports')}return <div><h2>{r.title}</h2><p>{r.content}</p><form action={action}><textarea name='content' className='border p-2'/><button>Reply</button></form></div>}
