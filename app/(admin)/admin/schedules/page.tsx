import Link from "next/link";import { prisma } from "@/lib/prisma";export default async function Schedules(){const s=await prisma.schedule.findMany({include:{course:true,teacher:{include:{user:true}}}});return <div><Link href='/admin/schedules/new'>Add schedule</Link><table>{s.map(x=><tr key={x.id}><td>{x.course.title}</td><td>{x.teacher.user.firstName}</td><td>{x.dayOfWeek}</td><td>{x.startTime}-{x.endTime}</td><td>{x.room}</td></tr>)}</table></div>}
