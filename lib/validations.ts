import { z } from "zod";
export const studentSchema = z.object({ firstName:z.string().min(1), lastName:z.string().min(1), email:z.email(), phone:z.string().optional(), gender:z.enum(["MALE","FEMALE","OTHER"]).optional(), dateOfBirth:z.string().optional(), address:z.string().optional(), guardianName:z.string().optional(), guardianPhone:z.string().optional(), emergencyContact:z.string().optional(), courseId:z.string().min(1), startDate:z.string().min(1), notes:z.string().optional() });
export const updateStudentSchema = studentSchema.partial().omit({courseId:true,startDate:true}).extend({id:z.string().optional()});
export const courseSchema = z.object({ title:z.string().min(1), slug:z.string().min(1), description:z.string().optional(), classType:z.enum(["PERSONAL","GROUP"]), durationWeeks:z.coerce.number().int().positive(), fee:z.coerce.number().positive(), thumbnail:z.string().optional(), isActive:z.coerce.boolean().default(true)});
export const teacherSchema = z.object({ firstName:z.string().min(1), lastName:z.string().min(1), email:z.email(), phone:z.string().optional(), gender:z.enum(["MALE","FEMALE","OTHER"]).optional(), specialty:z.string().optional(), bio:z.string().optional()});
export const scheduleSchema = z.object({ courseId:z.string().min(1), teacherId:z.string().min(1), dayOfWeek:z.coerce.number().int().min(1).max(7), startTime:z.string().min(1), endTime:z.string().min(1), room:z.string().optional()});
