"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { toast } from "sonner";import { replyToReport } from "@/lib/actions/admin";import { Textarea } from "@/components/ui/textarea";import { Button } from "@/components/ui/button";
export function ReplyForm({report}:{report:any}){const [content,setContent]=useState('');const r=useRouter();if(report.repliedAt)return <div className='rounded border p-4'>{report.replyContent}</div>;async function submit(){const res=await replyToReport(report.id,content); if(res.success) r.refresh(); else toast.error(res.error)}; return <div className='space-y-2'><Textarea value={content} onChange={e=>setContent(e.target.value)}/><Button onClick={submit}>Submit reply</Button></div>}
