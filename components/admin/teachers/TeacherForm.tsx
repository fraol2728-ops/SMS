"use client";import { useRouter } from "next/navigation";import { toast } from "sonner";import { createTeacher } from "@/lib/actions/admin";import { Input } from "@/components/ui/input";import { Textarea } from "@/components/ui/textarea";import { Button } from "@/components/ui/button";
export function TeacherForm(){const r=useRouter();async function submit(fd:FormData){const res=await createTeacher(fd); if(res.success) r.push('/admin/teachers'); else toast.error(res.error)} return <form action={submit} className='space-y-3 max-w-2xl'><Input name='firstName' placeholder='First name*'/><Input name='lastName' placeholder='Last name*'/><Input name='email' placeholder='Email*'/><Input name='phone' placeholder='Phone'/><select name='gender' className='h-10 rounded-md border px-3'><option value=''>Gender</option><option value='MALE'>MALE</option><option value='FEMALE'>FEMALE</option><option value='OTHER'>OTHER</option></select><Input name='specialty' placeholder='Specialty'/><Textarea name='bio' placeholder='Bio'/><Button type='submit'>Create teacher</Button></form>}
